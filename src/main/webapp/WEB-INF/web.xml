<?xml version="1.0" encoding="UTF-8"?>

<!-- Root element of the deployment descriptor -->
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

  <!-- Application Name -->
  <display-name>Zentutor Booking System</display-name>

  <!-- Default welcome file when application is loaded -->
  <welcome-file-list>
    <welcome-file>index.jsp</welcome-file>
  </welcome-file-list>

  <!-- Session configuration -->
  <session-config>
    <!-- Session will expire after 30 minutes of inactivity -->
    <session-timeout>30</session-timeout>

    <!-- Secure cookie settings -->
    <cookie-config>
      <!-- Cookies are not accessible via JavaScript (XSS protection) -->
      <http-only>true</http-only>

      <!-- Only send cookies over HTTPS (secure connection) -->
      <secure>true</secure>
    </cookie-config>

    <!-- Cookies are used to track sessions -->
    <tracking-mode>COOKIE</tracking-mode>
  </session-config>

</web-app>


